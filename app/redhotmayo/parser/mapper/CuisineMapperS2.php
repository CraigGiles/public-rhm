<?php namespace redhotmayo\parser\mapper;

class CuisineMapperS2 implements CuisineMapper {
    const C_CUISINE = 'cuisine';
    const C_SOURCE = 'source';
    const C_META = 'meta';

    const CUISINE = 'cuisine';
    const META_DATA = 'meta';

    /**
     * Obtain the cuisine type and meta data mapped to this value
     *
     * @param $value
     * @return array
     */
    public function map($value) {

    }
}